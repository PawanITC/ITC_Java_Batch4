version: "3.8"

services:

  # ======================
  # FRONTEND (React)
  # ======================
  client:
    build: ./client
    container_name: linkedin-client
    ports:
      - "3000:3000"
    volumes:
      - ./client:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true

  # ======================
  # BACKEND PLACEHOLDERS
  # (Spring Boot services later)
  # ======================

  # auth-service:
  #   build: ./server/auth-service
  #   ports:
  #     - "8081:8081"

  # user-service:
  #   build: ./server/user-service
  #   ports:
  #     - "8082:8082"

  # post-service:
  #   build: ./server/post-service
  #   ports:
  #     - "8083:8083"