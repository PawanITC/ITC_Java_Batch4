server:
  port: 8080

spring:
  application:
    name: connectionservice

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true