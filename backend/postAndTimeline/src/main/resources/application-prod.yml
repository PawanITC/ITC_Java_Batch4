spring:
  jpa:
    show-sql: false

  datasource:
    hikari:
      data-source-properties:
        sslmode: ${POSTGRES_SSLMODE:require}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
