server:
  port: 8085

spring:
  application:
    name: connections-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/linkedin-app}
          jwk-set-uri: ${KEYCLOAK_JWK_SET_URI:http://host.docker.internal:8080/realms/linkedin-app/protocol/openid-connect/certs}

  datasource:
    url: jdbc:postgresql://localhost:5432/connection_db
    username: connection_user
    password: connection_pass
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: ${SPRING_DATA_REDIS_HOST:localhost}
      port: ${SPRING_DATA_REDIS_PORT:6379}
      timeout: 2s

    cache:
      type: redis
      redis:
        time-to-live: 10m
        cache-null-values: false

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  kafka:
    bootstrap-servers: localhost:9092

    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.apache.kafka.common.serialization.StringSerializer
      acks: all
      retries: 3
      properties:
        enable.idempotence: true
        max.in.flight.requests.per.connection: 5

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
logging:
  level:
    root: INFO
    com.linkedinclone.connection: INFO
    org.hibernate.SQL: WARN
    org.springframework.security: WARN

connection-service:
  kafka:
    topics:
      connection-request-sent: connection.request.sent
      connection-request-accepted: connection.request.accepted
      connection-request-rejected: connection.request.rejected
      connection-request-cancelled: connection.request.cancelled
      user-blocked: connection.user.blocked
      user-unblocked: connection.user.unblocked
  outbox:
    publisher-delay-ms: 5000

