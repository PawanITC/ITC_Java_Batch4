apiVersion: v1
kind: Service
metadata:
  name: apigateway
  namespace: microservices
spec:
  type: NodePort
  selector:
    app: apigateway
  ports:
    - port: 8989
      targetPort: 8989
      nodePort: 30089